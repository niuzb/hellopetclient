#ifndef NUMBER_HPP_
#define NUMBER_HPP_

template<class NumType>
class RangeNum {
public:
	RangeNum(NumType max, NumType min, NumType num = NumType());

	NumType operator() ();	

private:
	NumType m_start;
	NumType m_end;
	NumType m_num;
}; 

#endif
